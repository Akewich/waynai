import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFFF8FAFC); // Clean off-white
  static const Color surface = Colors.white;
  
  // Brand Colors (Blue & Green)
  static const Color primaryBlue = Color(0xFF2563EB);   // Royal Blue
  static const Color secondaryBlue = Color(0xFF60A5FA); // Light Blue
  static const Color primaryGreen = Color(0xFF10B981);  // Emerald Green
  
  // RPG Accents (Adapted for Light Theme)
  static const Color expBar = Color(0xFF10B981);
  static const Color gold = Color(0xFFF59E0B); // Amber/Gold for rewards
  
  // Accents
  static const Color accentBlue = Color(0xFFEFF6FF);
  static const Color accentGreen = Color(0xFFECFDF5);
  static const Color accentRuby = Color(0xFFEF4444);
  
  // Text
  static const Color textPrimary = Color(0xFF1E293B);   // Slate 800
  static const Color textSecondary = Color(0xFF64748B); // Slate 500
}
